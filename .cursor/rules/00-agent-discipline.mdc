---
description: Core behavior rules for Cursor Agent.
alwaysApply: true
---

# Agent Discipline

- Inspect relevant files before editing.
- Do not invent APIs, file paths, environment variables, commands, or project structure.
- Prefer small, reviewable changes over large rewrites.
- Preserve existing architecture unless the task explicitly asks for refactoring.
- When uncertain, search the repository before assuming.
- Do not silently change unrelated files.
- Read `docs/TASKS.md` and stay within the current implementation phase.
- Do not introduce legacy Figma API grounding or DOM-selector-first automation.
- Record architecture decisions in `docs/DECISIONS.md`.
- After changes, summarize:
  - files changed
  - reason for each change
  - how to verify
  - known risks or unresolved questions
